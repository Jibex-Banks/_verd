name: verd
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # ===== Core =====
  # State management
  riverpod: ^3.2.1                # For providers
  riverpod_annotation: ^4.0.2      # Code generation (optional but helpful)
  flutter_riverpod: ^3.2.1

  # Navigation
  go_router: ^17.1.0               # Declarative routing

  # Localization
  flutter_localizations:
    sdk: flutter

  # Utilities
  intl: ^0.20.2                     # Date formatting
  url_launcher: ^6.3.2              # Open links (for help/terms)
  package_info_plus: ^9.0.0         # App version for about screen
  permission_handler: ^12.0.1       # Handle runtime permissions
  shared_preferences: ^2.5.4        # Local storage for settings
  uuid: ^4.5.1                       # Generate unique IDs for scans
  google_sign_in: ^6.3.0              # Google Sign-In

  # ===== Optional (add as needed) =====
  # Camera & gallery
  camera: ^0.12.0                   # Camera access for scan
  image_picker: ^1.2.1               # Pick images from gallery

  # TTS (Text-to-Speech) — REMOVED: unused, saves ~2-3MB native libs

  # UI enhancements
  flutter_svg: ^2.2.3                # SVG support
  shimmer: ^3.0.0                     # Shimmer loading effects
  # lottie: ^3.3.2                    # REMOVED: unused

  # Networking (if using Firebase or REST)
  # dio: ^5.9.2                        # REMOVED: unused (app uses Firebase SDKs)
  firebase_core: ^4.5.0                # Firebase core (if using Firebase)
  firebase_auth: ^6.2.0                # Firebase auth
  cloud_firestore: ^6.1.3              # Firestore database
  firebase_storage: ^13.1.0            # Firebase storage
  firebase_messaging: ^16.1.2          # FCM Push notifications
  firebase_analytics: ^12.1.3          # App analytics
  firebase_app_check: ^0.4.1+5         # Firebase App Check for security
  google_generative_ai: ^0.4.6         # Google Generative AI for Gemini features
  flutter_dotenv: ^5.1.0               # Load environment variables
  # mime: ^1.0.4                       # REMOVED: unused
  flutter_local_notifications: ^21.0.0  # For Android notification channels & foreground alerts

  # Offline-first & local caching
  hive: ^2.2.3                         # Core Hive DB
  hive_flutter: ^1.1.0                 # Hive Flutter bindings
  connectivity_plus: ^7.0.0            # Network state monitoring
  image: ^4.8.0                        # Image manipulation for TFLite
  
  # TensorFlow Lite for offline ML
  tflite_flutter: ^0.12.1             # TensorFlow Lite for Flutter
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  # cupertino_icons: ^1.0.8            # REMOVED: unused (app uses Material Icons)
  google_nav_bar: 5.0.7
  flutter_launcher_icons: ^0.14.4
  device_info_plus: ^12.3.0
  firebase_crashlytics: ^5.0.8


dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  mockito: ^5.6.3
  build_runner: ^2.12.2

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  generate: true

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - .env
    - assets/images/
    - assets/models/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
      - family: Inter
        fonts:
          - asset: assets/fonts/Inter-Regular.ttf
            weight: 400
          - asset: assets/fonts/Inter-Medium.ttf
            weight: 500
          - asset: assets/fonts/Inter-SemiBold.ttf
            weight: 600
          - asset: assets/fonts/Inter-Bold.ttf
            weight: 700
      # SF Pro font removed — unused, saves ~414KB
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.png"
  min_sdk_android: 21
